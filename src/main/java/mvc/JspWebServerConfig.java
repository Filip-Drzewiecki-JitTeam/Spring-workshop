package mvc;

import org.springframework.context.annotation.Configuration;

/**
 * Placeholder configuration for Spring MVC / JSP support.
 * When running via `mvn spring-boot:run` the Maven plugin automatically sets
 * src/main/webapp as the embedded Tomcat document root, so no extra setup is needed.
 */
@Configuration
public class JspWebServerConfig {
}

